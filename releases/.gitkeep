Manifests land here as releases/<yyyy>/<YYYY-MM-DD-HHMM>.json, one per Promote run.
This directory is the release ledger and the restore-point index. Restore points are
addressed by run timestamp, never by tag.
